# Alpha158 项目实施门禁

原则：每一阶段必须产出可检查的文件和通过条件；G1–G3 未通过时，不训练模型。

## G0 研究定义

- [x] 冻结主数据源与审计数据源的职责。
- [x] 冻结股票池、信号日、成交时点和标签。
- [x] 冻结 Alpha158、Ridge、LightGBM 的第一版边界。
- [x] 冻结滚动切分、中性化、评价与回测口径。
- [x] 配置通过 `validate_research_config.py` 校验。

产物：

- `README.md`
- `config/research_v1.json`
- `IMPLEMENTATION_CHECKLIST.md`
- `validate_research_config.py`

## G1 数据与历史股票池

- [x] 建立本项目只读的数据目录映射，不复制 11M 行行情。
- [x] 输出正式快照的字段、日期、行数、主键、缺失率审计表。
- [x] 建立周频信号日、建仓日、退出日映射。
- [x] 把沪深300月度权重还原为点时有效成分区间。
- [x] 核实 2015-05、2017-02、2025-03、2025-09 临时调样。
- [x] 检查每个信号日成分数量、重复成分和异常空窗。
- [ ] 验证申万2021一级行业与总市值均为 point-in-time 数据。
- [ ] 核对外部 DuckDB 仅用于诊断，未流入正式面板。

通过条件：

- 正式研究日期均能唯一映射信号、建仓和退出日。
- 每条成分记录具有明确的生效日与失效日，不使用当前成分回填。
- 临时调样均有可追溯依据。
- 任何缺失或异常都有显式日志，不静默补未来值。

建议产物：

- `artifacts/G1/data_audit.json`
- `artifacts/G1/weekly_calendar.parquet`
- `artifacts/G1/hs300_membership_intervals.parquet`
- `artifacts/G1/universe_audit.csv`

## G2 Alpha158 对齐

- [ ] 从 Qlib 官方定义锁定 158 个特征名和表达式。
- [ ] 明确 OHLC、VWAP、成交量和复权因子的转换口径。
- [ ] 实现仅依赖历史值的日频 Alpha158 计算。
- [ ] 在至少 10 只股票、20 个信号日上与 Qlib 参考结果对照。
- [ ] 验证输出恰好 158 列，列名和方向固定。
- [ ] 验证最早 60 日的可用性边界。
- [ ] 实现按信号日的缺失、MAD 缩尾和 Z-score。
- [ ] 记录全空列、零方差列、极端值和覆盖率。

通过条件：

- 特征数量严格等于 158。
- 表达式不含未来算子。
- 对照样本达到数值一致；若底层数据规范导致尺度不同，截面排序相关系数至少为 0.999999，并解释差异。
- 全量构建前，价格与成交量复权口径已在对照报告中锁定。

建议产物：

- `artifacts/G2/alpha158_catalog.csv`
- `artifacts/G2/qlib_parity_report.json`
- `artifacts/G2/feature_quality_by_date.parquet`

## G3 周频标签与无泄漏切分

- [ ] 生成个股建仓开盘到下一调仓开盘的收益。
- [ ] 用完全相同的日期区间计算沪深300收益。
- [ ] 生成个股相对指数的周度超额收益标签。
- [ ] 验证信号日信息早于建仓日。
- [ ] 验证退出日等于下一次调仓建仓日。
- [ ] 按 `label_end_date` 对训练、验证边界执行 purge。
- [ ] 固定五轮 5+1+1 年滚动切分。
- [ ] 输出每轮、每年、每周的样本量与标签分布。

通过条件：

- 抽样交易日期映射人工复核无误。
- 不存在跨越训练/验证或验证/测试边界的标签。
- 两个模型读取同一份冻结面板和 fold 标识。

建议产物：

- `artifacts/G3/weekly_model_panel/`
- `artifacts/G3/fold_manifest.json`
- `artifacts/G3/label_audit.json`

## G4 Ridge 基线

- [ ] 每轮只用训练集拟合、验证集选 alpha、测试集只预测一次。
- [ ] 保存模型参数、输入快照、预测值和运行元数据。
- [ ] 输出原始分数及行业市值中性化后的分数。
- [ ] 生成测试期周度 Rank IC 基线。

通过条件：

- 五个测试年份都有唯一、不重叠的样本外预测。
- 任何一次重跑均可由配置和数据快照复现。

## G5 LightGBM

- [ ] 使用与 Ridge 完全相同的输入和 fold。
- [ ] 仅搜索配置中限定的小型参数网格。
- [ ] 以验证集平均周度 Rank IC 选择参数。
- [ ] 使用固定随机种子并记录早停轮数。
- [ ] 在参数冻结后才生成测试集预测。

通过条件：

- 不存在测试集参与早停、选参或特征选择。
- 每轮保存最佳验证指标和最终参数。

## G6 信号评价

- [ ] 汇总周度 Rank IC 均值、标准差、ICIR 和胜率。
- [ ] 计算 Newey-West t 值及逐年 IC。
- [ ] 绘制累计 IC。
- [ ] 检查五分组收益单调性。
- [ ] 对 `IC_LightGBM - IC_Ridge` 做配对 Newey-West 检验。
- [ ] 对行业、规模和市场状态做稳定性切片。

通过条件：

- 主结论基于 2021–2025 拼接的真实样本外预测。
- 同时报告整体结果、逐年结果和模型差值，而非只挑最好年份。

## G7 可交易回测

- [ ] 实现 Top50 等权周度组合。
- [ ] 实现停牌、涨跌停和失败卖出状态机。
- [ ] 现金、持仓与成本按实际成交更新。
- [ ] 跑单边 0、10、20 bps 成本情景。
- [ ] 输出超额收益、IR、Sharpe、回撤、换手和成本。
- [ ] 输出行业及市值暴露。
- [ ] 将 Top-Bottom 明确标为信号诊断，不冒充可实施多空产品。

通过条件：

- 交易日志能够解释任一周的目标仓位、实际成交和遗留持仓。
- 净值可由成交日志逐日复算。
- 成本后结果与信号评价方向一致，若不一致则明确解释。

## G8 报告与面试材料

- [ ] 生成完整实验报告与复现命令。
- [ ] 固化数据谱系、参数、随机种子和软件版本。
- [ ] 准备 1 页项目摘要和 3–5 分钟口述版本。
- [ ] 准备前视偏差、幸存者偏差、中性化、IC、换手、成本等追问。
- [ ] 明确负结果和局限，不包装成实盘业绩。

最终可写入简历的叙事：

> 在沪深300历史成分股上复现 Alpha158，构建无前视的周频滚动训练与样本外评价框架；比较 Ridge 与 LightGBM 的截面排序能力，并在行业市值中性化、涨跌停/停牌约束及多档交易成本下验证指数增强组合的稳定性。

只有 G6、G7 实际结果完成后，才补充具体 IC、IR、超额收益和回撤数字。
